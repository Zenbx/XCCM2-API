# =============================================================================
# XCCM2 API - Environment Variables Template
# =============================================================================
# IMPORTANT: Ne jamais committer le fichier .env avec de vraies valeurs !
# Copiez ce fichier en .env et remplacez toutes les valeurs par vos vraies clés
# =============================================================================

# Database
# Obtenir votre URL MongoDB Atlas sur https://cloud.mongodb.com
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/database-name?retryWrites=true&w=majority"

# JWT Configuration
# Générer avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-minimum-32-characters-required"
JWT_EXPIRES_IN="7d"

# Application
NODE_ENV="development"
NEXT_PUBLIC_API_URL="http://localhost:3000"

# CORS Configuration
# Liste des origines autorisées (séparées par des virgules)
# Développement: http://localhost:3001,http://localhost:3000
# Production: https://app.votre-domaine.com,https://api.votre-domaine.com
ALLOWED_ORIGINS="http://localhost:3001,http://localhost:3000,http://127.0.0.1:3001,http://127.0.0.1:3000"

# Hugging Face AI - Chat Bot
# Créer un compte sur https://huggingface.co et obtenir un token
HF_API_TOKEN="hf_your_huggingface_api_token_here"

# Email Configuration (SMTP)
# Utiliser Gmail: https://support.google.com/accounts/answer/185833
# Ou un autre fournisseur SMTP
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-specific-password"
FROM_EMAIL="Your App Name <your-email@gmail.com>"

# Supabase Storage Configuration
# Créer un projet sur https://supabase.com
SUPABASE_URL="https://your-project-id.supabase.co"
SUPABASE_ANON_KEY="your-supabase-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"
SUPABASE_BUCKET_NAME="xccm-documents"

# Upstash Redis (REST API)
# Créer une base de données sur https://upstash.com
UPSTASH_REDIS_REST_URL="https://your-redis-instance.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-upstash-redis-token"

# Ably (Real-time WebSockets)
# Créer un compte sur https://ably.com
ABLY_API_KEY="your-ably-api-key"

# NextAuth Configuration
# L'URL publique de votre application frontend
NEXTAUTH_URL="http://localhost:3001"
# Générer avec: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-minimum-32-characters"

# OAuth2 Configuration

# Google OAuth
# Console: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:3001/api/auth/callback/google"

# Microsoft OAuth
# Portail: https://portal.azure.com/#view/Microsoft_AAD_RegisteredApps
MICROSOFT_CLIENT_ID="your-microsoft-client-id"
MICROSOFT_CLIENT_SECRET="your-microsoft-client-secret"
MICROSOFT_REDIRECT_URI="http://localhost:3000/api/auth/callback/azure-ad"

# Socratic AI (Hugging Face)
# Même token que HF_API_TOKEN ou un token séparé
HUGGING_FACE_API_KEY="hf_your_huggingface_api_key_for_socratic_ai"
