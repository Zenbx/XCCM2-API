# =============================================================================
# SYNAPSE WEBSOCKET SERVER - Configuration Template
# =============================================================================
# Ce fichier contient toutes les variables d'environnement nécessaires pour
# déployer le serveur WebSocket Synapse (Hocuspocus) séparément du backend API.
#
# INSTRUCTIONS:
# 1. Copiez ce fichier et renommez-le en ".env" pour le développement local
# 2. Sur Railway, ajoutez ces variables dans Settings > Variables
# 3. Générez des secrets sécurisés avec: openssl rand -base64 32
# =============================================================================

# -----------------------------------------------------------------------------
# 🔌 Port du Serveur WebSocket
# -----------------------------------------------------------------------------
# Railway définit automatiquement PORT, vous n'avez pas besoin de le configurer
# En local, utilisez 1234 ou tout autre port disponible
PORT=1234

# -----------------------------------------------------------------------------
# 🗄️ Base de Données MongoDB
# -----------------------------------------------------------------------------
# URL de connexion à MongoDB Atlas
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
#
# IMPORTANT: Utilisez la MÊME base de données que votre backend API
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/xccm2"

# -----------------------------------------------------------------------------
# 🔐 Authentification JWT
# -----------------------------------------------------------------------------
# Secret pour vérifier les tokens JWT (DOIT être identique au backend)
# Minimum 32 caractères pour HS256
#
# ⚠️ CRITIQUE: Ce secret DOIT être exactement le même que dans le backend API
# Générez-le avec: openssl rand -base64 32
JWT_SECRET="votre-secret-jwt-32-caracteres-minimum-identique-au-backend"

# -----------------------------------------------------------------------------
# 🌍 Environnement
# -----------------------------------------------------------------------------
# Mode d'exécution: "production" en déploiement, "development" en local
NODE_ENV=production

# -----------------------------------------------------------------------------
# 📊 Logs & Debug (Optionnel)
# -----------------------------------------------------------------------------
# Niveau de logs: "info", "warn", "error", "debug"
LOG_LEVEL=info

# Activer les logs détaillés de Prisma (utile pour le debug)
# DEBUG=prisma:*
